Pushed back on Vector DB, Alyssa mentioned we already have centurianCDKwiki - kinda similar vectorless RAG, also why this project - we’re giving claude start points to search (again pre filtering - like normal english language to kb formatable (search query with parameter should take care by claude or consumer), then so semantic search in kb also find relevant chunk , pool like top 50 then post processing - custom reranking this should taken care by again claude or consumer), also she mentioned like we need to maintain page (like how tickets should be chunked and how to relate chunks like this page - not sure she means group of chunks, if there is any terminology), she also mentioned on example, like if user ask kb_search - what’s current state of vpref - then kb_search should do semantic search fan out agents and get relevant tickets preprossong query from claude with metaada sorting by created_at field or updated_at field, then pool bunch of relevant tickets, relevant tasks from taskei, related wiki/runbooks or related and indexed code - claude with run actual thing by using source_uri in metadata stored chunks and gives actual current state or relevat start point of claude code to do internal page reading tool to fetch exact details, also chunking - logical ? semantical ? how code indexed in gitfarm ? mayebe (module wise/logic wise) - then index with repo, class, file, in chunk metadata, heirarchical or heuristically save chunks
